  mov %15, =1
  breq %16, %15, Lf
  mov %16, %15
  call If

L:  # Loop
  add %14, =1
  mov %15, =2
  brne %14, %15, Ff
  mov %14, =0

  mov %8, =64
  ld %9, %8
  xor %9, =0xFFFF
  st %9, %8

F:  # Finish
  sc =0

I:  # Initialise
  mov %8, =64
  # Fill 24 lights with 0x66ccff
  mov %9, =0
  mov %10, =24
  mov %11, =0xccfF
  add %11, =0x660000
A:
  add %12, %8, %9
  st %11, %12
  add %9, =1
  brlt %9, %10, Ab
  ret
